/**
 * Newsletter signup stub.
 *
 * The articles section now renders a Substack CTA inline, so this component
 * intentionally returns null. Kept in-tree to avoid breaking the import in
 * any page that still references it. Remove this file once all imports are
 * cleaned up.
 */
export function NewsletterSignup() {
  return null
}
